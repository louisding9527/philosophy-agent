# 复制为 .env 后填写（.env 已被根 .gitignore 的 `.env` 规则忽略，不会提交）
# 注意：这里的密码要和 backend/.env 里的 DATABASE_URL / NEO4J_URI 保持一致
POSTGRES_USER=philosophy
POSTGRES_PASSWORD=改成你自己的密码
POSTGRES_DB=philosophy
NEO4J_PASSWORD=改成你自己的密码
